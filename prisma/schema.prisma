datasource db {
  provider = "sqlite"
  url      = "file:./THE-DATABASE--GASP/kiwindb"
}

generator client {
  provider = "prisma-client-js"
}

model Server {
  id     String @id
  prefix String @default("!")
}
